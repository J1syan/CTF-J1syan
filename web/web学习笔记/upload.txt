1、前端js验证：
抓包后，修改类型

2、content-type 验证
抓包后，修改content-type 类型

3、黑名单绕过（常见的文件后缀）
使用其他的文件类型上传，但要符合服务器配置文件

4、.htaccess重写绕过
描述:禁止任何可执行文件上传，但是可以上传.htaccess，
FilesMatch "jpg">
SetHandler application/x-httpd-php
</FilesMatch>
即：图片当作php代码解析

5、文件名加.(空格).绕过


6、文件大小写绕过
黑名单的文件后缀不够多

7、文件名加空格绕过
后端未使用 trim（）

8、文件名加.绕过
后端未使用 deldot()函数

9、::$DATA绕过
在window的时候如果文件名+"::$DATA"会把::$DATA之后的数据当成文件流处理,不会检测后缀名，
且保持::$DATA之前的文件名，他的目的就是不检查后缀名

10、文件名加.(空格).绕过

11、str_ireplace(）过滤后缀
.pphphp绕过

12、%00 截断绕过上传
$_FET['save_path']服务器接收客户端的值，可以被客户端修改
%00对目录或者文件名进行截断
条件:
gpc关闭；php版本小于5.3.4

13、与12相似，不同的是使用post，进行传参，需要自行进行编码转码

14、


